Build & Run - Watch
dotnet watch run

DataBase 
dotnet ef migrations add "NameOfTheMigrations"

dotnet ef update database

dotnet tool install --global dotnet-ef

User Secrets
dotnet user-secrets init

dotnet user-secrets set "<Name>" "Value"

dotnet user-secerts set "<ConnectionStrings>:<ConnectionString>" "ConnectionString Data"

dotnet user-secrets list

Dotnet - 

dotnet new webapi "<ProjectName>" --no-https


Angulars
npm install -g @angular/cli

ng new "Name of the Project"

ng server --open

ng Build

ng g c "ComponentName"

ng g m "ModuleName"

ng g s "ServiceName"

npm i bootstrap --save 

npm root -g 

npm install -g typescript@latest

npm g interface "InterfaceName"

npm install alertifyjs --save

npm install ngx-bootstrap --save

ng update @angular/core @angular/cli

npm install @Kolkov/ng-gallery

ng g p "pipeName"

ng build --prod

npm install -g firebase-tools

firebase login

firebase init

firebase deploy

ng add @angular/fire

ng deploy

Extensions : 
nuget gallery

